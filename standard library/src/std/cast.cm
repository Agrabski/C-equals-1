import {usize} from {cm1mLang}
import {ReplaceWithLLVMIR} from {std::compiler}
namespace std
{
	[ReplaceWithLLVMIR("sizeof.llvm", "T", nameof(T), "Y", nameof(Y))]
	public fn unsafe_cast<T, Y>(i: T*) -> Y*
	{}
}