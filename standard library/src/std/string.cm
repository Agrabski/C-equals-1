
namespace std
{
	class dyn_string
	{
		private _text : char[];
		
		public construct()
		{
			_text = new_array<char>(0);
		}

		public construct(text: string)
		{
			_text = new_array<char>(text.length());
			for(let index in enumerate(0, text.length()))
				_text[index] = text[index];
		}
	}
}