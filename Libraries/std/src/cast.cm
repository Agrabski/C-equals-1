import {usize} from {cm1mLang}
import {ReplaceWithLLVMIR} from {compiler}

[ReplaceWithLLVMIR("sizeof.llvm", "T", nameof(T), "Y", nameof(Y))]
public fn unsafe_cast<typename T, typename Y>(i: T*) -> Y*
{
	return null<Y>();
}