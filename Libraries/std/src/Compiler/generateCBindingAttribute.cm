import {functionDescriptor} from {cm1mLang::compiler}
import {getOutputDirectory open write} from {cm1mLang::filesystem}

namespace compiler 
{
	public att<function> GenerateCBinding
	{
		public fn attach(f: functionDescriptor)
		{

		}
	}
}
