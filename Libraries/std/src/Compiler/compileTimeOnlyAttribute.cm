import {markCompileTimeOnly functionDescriptor} from {cm1mLang::compiler}

namespace compiler 
{
	public att<function> CompileTimeOnly
	{
		public fn attach(f: functionDescriptor)
		{
			markCompileTimeOnly(f);
		}
	}